apiVersion: v1
kind: Service
metadata:
  name: notification-api
  namespace: notifications
spec:
  selector:
    app: notification-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
